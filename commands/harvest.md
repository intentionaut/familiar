---
description: "Familiar: Extract patterns across projects for low-energy days"
---
<!-- Generated by scripts/build-plugin.py from .claude/commands/. Edit the adapter there, not this file. -->
Run the Familiar **harvest** stage. Familiar lives at `${CLAUDE_PLUGIN_ROOT}`.

1. Read `${CLAUDE_PLUGIN_ROOT}/AGENTS.md` for the rules.
2. **Knowledge lives at the folder that `python3 ${CLAUDE_PLUGIN_ROOT}/scripts/paths.py --knowledge-only` prints.** Every `knowledge/<file>.md` a prompt names is read from there, not from the Familiar folder. If that path ends in the repo's own `knowledge/`, those are the shipped templates: say so and do not edit against them.
3. Read `${CLAUDE_PLUGIN_ROOT}/prompts/harvest.md` and follow every instruction in it exactly. It names the knowledge files you must read first.
4. Arguments: $ARGUMENTS
5. Write the result to `knowledge/patterns.md` in the resolved knowledge folder.
6. Report what was found and suggest next steps.
